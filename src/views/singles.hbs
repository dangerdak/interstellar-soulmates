{{>signOut}}
<section>
    <ul>
        {{#each singles}}
        <li>
          <a href="/profile/{{this.user_id}}">
            <img src="{{this.image_url}}" alt="profile picture for {{this.name}}">

                <h3>{{this.name}}</h3>
            </a>
        </li>
        {{/each}}
    </ul>
</section>
